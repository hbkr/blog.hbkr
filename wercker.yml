box: wercker/default
build:
  steps:
    - arjen/hugo-build@1.8.1
        theme: angels-ladder
        flags: --buildDrafts=true
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: hbkr.github.io
        basedir: public
